import { configDataState } from "../models/store.model";

export const configDataFeatureKey = 'Config';

export const partFeatureKey = 'Part';

export const initialConfigDataState: configDataState = {
  objects: [],
}